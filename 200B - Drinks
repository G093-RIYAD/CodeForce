#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, a[100];
    cin >> n;
    float sum = 0;
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
        sum += a[i];
    }
    cout << sum / n << endl;
    return 0;
}
